{
  "name": "cardata-lookup-scrapping",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "build": "npm run clean && tsc",
    "build:lambda": "npm run clean && npm run build && cp package.json dist/ && cd dist && npm install --production --legacy-peer-deps && zip -r ../lambda-deployment.zip . && cd ..",
    "start": "node dist/server.js",
    "start:prod": "NODE_ENV=production node dist/server.js",
    "clean": "rm -rf dist",
    "build:clean": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@codegenie/serverless-express": "^4.17.0",
    "@infosimples/node_two_captcha": "^1.2.0",
    "@sparticuz/chromium": "^119.0.0",
    "@zennolab_com/capmonstercloud-client": "^1.9.0",
    "cloudinary": "^2.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "puppeteer": "^24.4.0",
    "puppeteer-core": "^22.6.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-user-preferences": "^2.4.1",
    "puppeteer-screen-recorder": "^3.0.6"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.10",
    "@types/puppeteer": "^5.4.7",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
